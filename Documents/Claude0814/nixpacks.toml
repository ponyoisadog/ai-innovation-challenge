[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Static site - no build needed'"]

[start]
cmd = "npx serve -s . -l $PORT"

[variables]
FLASK_ENV = "production"